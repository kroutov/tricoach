NODE_ENV=development
PORT=3000
DATABASE_URL=postgresql://tricoach:tricoach@localhost:5432/tricoach?schema=public
TEST_DATABASE_URL=postgresql://tricoach:tricoach@localhost:5432/tricoach_test?schema=public
JWT_SECRET=change-me
JWT_EXPIRES_IN=30d
APPLE_BUNDLE_ID=com.tricoach.ai
# Comma-separated list of web origins allowed to call the API (e.g. the Vite dev server, the deployed frontend).
CORS_ORIGINS=http://localhost:5173
# The web app's own public URL — OAuth callbacks (Strava) redirect the browser here.
WEB_PUBLIC_URL=http://localhost:5173
STRAVA_CLIENT_ID=
STRAVA_CLIENT_SECRET=
STRAVA_WEBHOOK_VERIFY_TOKEN=
